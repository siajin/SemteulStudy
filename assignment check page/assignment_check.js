


/*
const list_upload = document.getElementById("list_upload");
const list = document.getElementById("list");
const calendar = document.getElementById("calendar");
const code_view = document.getElementById("code_view");

function viewListUpload(){
  ReactDom.render(<ListUpload />, list_upload);
}

function ListUpload (){
  const [date, setDate] = React.useState("");
  const [assignment, setAssignment] =
  return{
    <input type=""
    <input type=""
    <input type="file">
    <button onClick="Save">Save</button>
  };
}
z
function Save(){
  ReactDom.render(<AssignmentDate />, list);
}

function AssignmentDate(){
  return (
    <div onClick={UploadToList}>{date}</div>
  );
}

function UploadToList(){
   ReactDom.render(<ListUpload />, list_upload);
}
function
*/
